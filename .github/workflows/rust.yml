on:
  workflow_dispatch:
    inputs:
      release_version:
        description: "Release version"
        required: true

jobs:
  release:
    name: release ${{ matrix.target }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-pc-windows-gnu
            archive: zip
          - target: x86_64-unknown-linux-musl
            archive: tar.gz tar.xz
          #- target: x86_64-apple-darwin
          #  archive: zip
    steps:
      - uses: actions/checkout@master
      - name: Compile
        env:
          RUSTTARGET: ${{ matrix.target }}
        run: |
          cargo install cross
          cd Hookstaller
          cross build --release --target $RUSTTARGET
      - name: Create Release
        run: gh release create ${{ github.events.inputs.release_version }} ./target/release/* --pre-release

        
